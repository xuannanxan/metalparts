获取验证码
---
parameters:
  - name: image_code
    in: path
    type: string
    required: true
responses:
  200:
    description: 返回验证码图片（base64）
    schema:
      properties:
        data:
          type: string
  400:
    description: 获取验证码失败
    schema:
      properties:
        msg:
          type: string
 

